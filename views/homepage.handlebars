{{#each topic as |Topic| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <li>{{{get_emoji}}}<a href="/topic">{{Topic.name}}</a></li>
  </div>
  <div class="col-md-6">
    <h3>{{Topic.description}}</h3>
  </div>
</div>
{{/each}}

        {{!-- {{#if logged_in}} --}}

        {{!-- {{/if}} --}}